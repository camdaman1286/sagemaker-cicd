apiVersion: v1
kind: Service
metadata:
  name: cams-fraud-detection-svc

  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
    service.beta.kubernetes.io/aws-load-balancer-type: external
spec:
  type: LoadBalancer
  selector:
    app: cams-fraud-detection
  ports:
    - port: 80
      targetPort: 8000
